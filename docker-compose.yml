services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: photosite
      POSTGRES_PASSWORD: photosite
      POSTGRES_DB: photosite
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
